# File: /trade-weaver/pytest.ini
[pytest]
# Restricts pytest to only look for tests in the specified directory.
# This improves performance and prevents accidental discovery of tests elsewhere.
testpaths = tests

markers =
    integration: marks tests as integration tests (slow, requires network)